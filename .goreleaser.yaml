project_name: redactyl

before:
  hooks:
    - go mod tidy

builds:
  - id: redactyl
    main: .
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/redactyl/redactyl/cmd/redactyl.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    binary: redactyl

archives:
  - id: default
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc

release:
  github:
    owner: redactyl
    name: redactyl
  draft: false
  prerelease: auto

nfpms:
  - id: default
    package_name: redactyl
    formats: [deb, rpm, apk]
    maintainer: maintainer: Franz <maintainer@redactyl.io>
    description: Find secrets in your repo with low noise.
    license: Apache-2.0
    homepage: https://github.com/redactyl/redactyl
    section: utils
    priority: optional
    contents: []
    bindir: /usr/bin

